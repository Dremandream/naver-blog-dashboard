# plan.md — 소스 적중률 (로드맵 §8-4, 대형)

## 목표
소스(필자)가 강세/약세 의견을 낸 종목의 실제 성과를 추적 → 소스별 신뢰 점수.
"이 필자 말을 얼마나 믿을 수 있나"를 데이터로.

## 확정 스펙 (2026-07-17 사용자 결정)
- **적중 기준**: 지수 대비 초과수익 (benchmark-relative). 시장 전체가 오른 날의 운을 걸러 실력만 측정.
  - 강세 의견 → (종목 수익률 − 지수 수익률) > 0 이면 적중
  - 약세 의견 → (종목 수익률 − 지수 수익률) < 0 이면 적중
  - 중립 의견은 적중률 집계 제외
- **평가 기간**: 1개월·3개월·1년 (거래일 기준 21·63·252칸 뒤). ※ 2026-07-21 사용자 요청으로 5·20일 → 확장. 1년은 history가 252거래일 쌓여야 성숙 → 그전엔 "결과 대기".
- **벤치마크**: KR 종목 → KOSPI(코스닥주면 KOSDAQ). ⚠️ **US 종목은 현재 추적 지수 없음** → phase 2에서 ^GSPC/^IXIC 지수 수집 추가 필요. 그전까지 US 종목은 절대수익 폴백 또는 집계 제외(구현 시 결정).

## Phase 1 — 데이터 축적 + 소급 백필 (이번 세션 완료 ✅)
- `fetchClosesDated()`/`fetchIndexClosesDated()` — 네이버 시세 API가 원래 45일치 일봉을
  받아오는데 날짜를 버리고 등락률만 쓰던 걸, 날짜 보존 버전으로 추출. fetchPrices/fetchMarketData가
  `_closes`(날짜별 종가 시리즈)를 실어 나름 → posts.json 저장 직전 제거(이력 파일 전용).
- `archiveHistory()` → `public/data/history.json`에 **날짜 정확** upsert:
  ```
  { "YYYY-MM-DD": {
      "prices": { "종목명": 종가, ... },        // 그날 전 종목 종가(미래 T+N 조회용)
      "indices": { "KOSPI": n, "KOSDAQ": n },
      "opinions": [ { person, stock, stance, market } ]  // 그날 의견, person+stock 중복제거
  } }
  ```
- **핵심**: `_closes` 시리즈로 posts.json에 남은 의견 구간(약 8일)을 날짜 정확하게 채움 →
  **매 실행이 자동 백필**. 별도 백필 스크립트 불필요. 실행 즉시 07-10~07-16 5거래일 확보됨.
- 가격 없는 종목의 의견은 제외(판정 불가). 오늘(장 마감 전)은 종가 미확정이라 다음 실행에 완성.
- `.github/workflows/collect.yml`에 history.json git add 추가 (fresh checkout 소실 방지).
- 최근 120일치 유지.

## Phase 2 — 적중 판정 (이번 세션 완료 ✅)
- `scripts/hitrate.js` (judge.js처럼 순수 JS, AI 미사용, 결정적):
  - 각 의견 (person, stock, stance, date T)에 history의 정렬 거래일 리스트에서 T+5·T+20칸 뒤 조회
  - 초과수익 = 종목수익률 − 벤치마크(KR=KOSPI, US=NASDAQ)수익률. 강세는 >0, 약세는 <0 이면 적중.
  - T 또는 T+N 종가/지수 결손 시 pending(집계 제외). person별 {hits,total,rate}×{5,20}.
  - MIN_SAMPLE=5 미만은 rate=null(UI 표본부족). 중립 제외.
- **US 벤치마크 지수 수집 추가**: fetchForeignIndexClosesDated로 나스닥(.IXIC)·S&P500(.INX) → market·history.indices에 NASDAQ/SP500 저장.
- posts.json에 `source_scores` 저장. 골든 케이스 7건 회귀테스트 추가(HR1~HR7, 총 38건 통과).

## Phase 3 — UI (이번 세션 완료 ✅)
- `src/components/SourceScores.jsx` — AttentionTrends 아래 배치. 소스별 의견수·5일·20일 적중.
- 적중률(표본충족)순 정렬. total<min은 "표본부족", 미성숙은 "판정중" 표기(침묵 절삭 금지).
- 전 소스 미성숙(오늘)이면 상단에 추적 시작 안내 배너. **"매매추천 아님, 사후 성과 기록" 명시.**

## Phase 4 — 소급 백필 (이번 세션 완료 ✅)
- `scripts/backfill-history.js` (1회성): git 이력의 posts.json 커밋 스냅샷 union → 과거 (날짜·인물·종목·stance) 복원 → 방향성 종목의 과거 종가 + 4개 지수 수집 → history.json 병합 → source_scores 재계산해 posts.json 반영.
- **한계**: 방향성 stance는 분석기가 2026-07-06부터 추출 → 07-06 이전 의견은 전부 중립(집계 제외). 실질 적중률 백필 범위 = 07-06~07-17.
- `archiveHistory` 덮어쓰기→병합으로 변경(일일 실행이 백필분을 지우지 않게).
- 결과(07-17 시점): 콤디티 40%(2/5)·의교창 20%(2/10) 실수치 표시. 나머지 표본부족/판정중. 20일창은 미도래(전부 판정중) → 8월 초부터 채워짐.

## 상태
- "전부 판정중" 해소. 실데이터 적중률이 소스별로 표시되며 매일 확대됨.
- 로직은 골든 케이스 + 실데이터로 검증 완료. Playwright로 UI 렌더 확인.

## Phase 5 — 소스 우선 탐색 UI (2026-08-07 완료 ✅)
- [x] 헤더 다음에 소스 적중률을 우선 배치한다.
- [x] 표본 수를 반영한 신뢰 보정 순위로 작은 표본의 과대평가를 막는다.
- [x] 종합 신뢰도·1년 적중률·3개월 적중률 정렬을 제공한다.
- [x] 상위 5개 소스에 최근 7일 관련글을 소스별 최대 2개 연결한다.
- [x] 소스 클릭 시 아래 개별 글 목록을 해당 필자로 필터링한다.
- [x] 순위·관련글 규칙 회귀 테스트, 전체 테스트, 빌드, 데스크톱·모바일 UI를 검증한다.

## Phase 6 — 30초 원문 선별 대시보드 (2026-08-09 완료 ✅)

### 사용자 목표
- 사용 시점: 장 시작 전·장 마감 후
- 30초 안에 확인할 것: 새로운 투자 아이디어와 읽을 가치가 높은 원문
- 소스 신뢰 기준: 종목 의견의 1년 적중률(표본 수 보정)
- 관심 종목: 삼성전자·SK하이닉스
- 반대 의견: 강세·약세의 가장 강한 근거와 각 소스 신뢰도를 함께 표시
- 성공 기준: 네이버에서 직접 읽는 글 수와 하루 분석 시간 감소

### 이번 구현 범위
- [x] 최근 글에서 투자 무관 글을 제외하고 서로 다른 아이디어 원문 3개를 선별한다.
- [x] 관심 종목별 최근 강세·약세 근거와 소스의 1년 신뢰도를 표시한다.
- [x] 동일 종목에 강세·약세가 공존하면 양쪽 최강 근거를 비교한다.
- [x] 장 시작 전·장중·장 마감 후 이용 맥락을 화면에 표시한다.
- [x] 소스 순위의 기본 기준을 1년 적중률로 변경한다.
- [x] 순수 선별 로직을 회귀 테스트하고 빌드·데스크톱·모바일 화면을 검증한다.

### 후속 범위
- 상위 신뢰 소스 새 글 알림은 발송 채널·기준 확정 후 구현한다.
- 피터케이 극단적 공포의 5거래일 후 성과는 일별 지수 시계열을 축적한 뒤 검증한다.

## Phase 7 — 선별 정확성 재검토 (2026-08-09 완료 ✅)
- [x] 신규 아이디어 3개가 가능하면 서로 다른 검증 소스에서 나오도록 편향을 줄인다.
- [x] 관심 종목 근거는 해당 종목을 직접 언급한 글을 우선하고, 간접 근거는 명시한다.
- [x] 의견 충돌은 서로 다른 필자의 강세·약세가 존재할 때만 표시한다.
- [x] 원문 클릭 수를 날짜별·기기별로 저장해 실제 사용량을 확인한다.
- [x] 실데이터 결과, 회귀 테스트, 빌드, 데스크톱·모바일 UI를 재검증한다.

## Phase 8 — 오늘 새로 볼 것 1+2+1 (2026-08-09 완료 ✅)

### 확정 UI
- 첫 화면: 관심 종목 중대 변화 최대 1개 → 완전 신규 최대 2개 → 재부상 최대 1개.
- 조건을 만족하지 않으면 빈자리를 억지로 채우지 않는다.
- 모바일은 큰 카드 대신 압축 목록, 원문으로 바로 이동, 열어본 항목은 흐리게 유지한다.
- 관심 종목은 요약만 보이고 터치하면 근거를 펼친다.
- 의견 충돌은 서로 다른 필자를 데스크톱 좌우·모바일 위아래로 비교한다.
- 첫 화면은 앱형, 상세 리포트는 기존 증권사 리포트 디자인을 유지한다.

### 판정 기준
- 완전 신규: 최근 30일 종목 언급 없음 + 구체 근거/촉매 + 원문 존재.
- 재부상: 최근 30일 언급 이력이 있는 종목에 새로운 촉매 발생.
- 관심 종목 중대 변화: 검증 소스의 시각 전환, 새 촉매, 검증 소스 2명 이상 방향 합치, 의견·주가 엇갈림 중 2개 이상. 검증 소스의 뚜렷한 시각 전환은 단독 허용.
- 신뢰도는 1년 적중률과 표본 수를 원자료로 표시하며 미검증 소스는 검증 소스보다 뒤에 둔다.

### 구현
- [x] 프론트에 18MB history.json을 내려받지 않고 수집기가 45일 경량 언급 이력만 posts.json에 저장한다.
- [x] 기존 AI 분석 호출에 catalyst 한 문장만 추가한다(추가 API 호출 없음).
- [x] 1+2+1 선별 로직을 순수 함수와 회귀 테스트로 고정한다.
- [x] 오늘 원문 수·선별 비율·최근 7일 이용일·원문 클릭 수를 기기 로컬에서 표시한다.
- [x] 기존 상세 리포트·필터·소스 적중률 기능을 보존하고 데스크톱 UI·480px 반응형 규칙을 검증한다.

## Phase 9 — 종합의견 중심 원문 선별기 (2026-08-09 완료 ✅)

### 사용자 목표
- 모든 글을 읽지 않고도 첫 화면에서 오늘 시장의 종합의견을 파악한다.
- 종합의견 중 중요하거나 특이한 의견만 근거 원문으로 이동한다.
- 특정 선호 블로그에 먼저 편향되지 않고 전체 소스를 비교하되, 1년 적중률과 표본은 원문 우선순위에 반영한다.

### 구현
- [x] 종합 리포트와 팩트 사이드바를 첫 화면 최상단으로 이동한다.
- [x] 소수·역발상, 긍정 핵심, 부정 핵심 의견에 대응하는 원문을 최대 4개 연결한다.
- [x] 같은 원문 중복과 종합의견에 대응하지 않는 원문을 제외한다.
- [x] 기존 1+2+1·관심 종목·의견 충돌은 종합의견 아래 보조 영역으로 보존한다.
- [x] 원문 매칭 회귀 테스트, 빌드, 실데이터, 데스크톱·모바일 반응형 규칙을 검증한다.

## Phase 10 — 국내 시세 공개 JSON 우선 전환 (2026-08-12 완료 ✅)

### 목적
- 국내 종목 시세와 국내 지수 최신값을 API 키 없는 공공데이터 가공 JSON에서 우선 가져온다.
- 외부 서비스의 지연·중단·스키마 변경이 대시보드 전체 실패로 번지지 않게 기존 네이버 경로를 폴백으로 유지한다.
- Claude 요약은 기존 GitHub Secret `CLAUDE_API_KEY`를 그대로 사용한다.

### 범위와 완료 기준
- [x] `s/{code}_history.json`의 날짜·종가 배열을 검증하고 현재 적중률 형식 `{date, close}`로 변환한다.
- [x] `today.json`의 KOSPI·KOSDAQ 최신 확정값을 기존 지수 이력에 안전하게 병합한다.
- [x] 기준일이 8일 이상 지난 데이터, 잘못된 종목코드, 빈 행·잘못된 수치는 거부한다.
- [x] 공개 JSON 실패 시 국내 종목·지수 모두 기존 네이버 수집으로 자동 복귀한다.
- [x] 해외 종목·해외 지수·투자자별 수급·Claude AI 요약은 변경하지 않는다.
- [x] 회귀 테스트 100/100과 프로덕션 빌드, 삼성전자·SK하이닉스·KOSPI·KOSDAQ 실응답을 검증한다.

### 의도적으로 하지 않는 것
- MCP·n8n·새 API 키를 추가하지 않는다.
- 공시 UI와 AI 프롬프트를 이번 변경에 섞지 않는다.
- 공개 JSON이 제공하지 않는 국내 지수 장기 이력은 기존 네이버 이력을 유지한다.

## Phase 11 — 개인 리서치 인박스: 오늘 꼭 읽을 글 3개 (2026-08-12 완료 ✅)

### 목적
- 종합의견을 읽은 뒤 모든 글을 훑지 않고도, 실제로 원문을 열 가치가 높은 글 3개를 바로 고른다.
- 프리폭스 사례의 개인화 홈·큰 핵심 카드·명확한 행동 버튼 구조를 참고하되 현재 증권사 리포트 톤을 유지한다.
- 새 모델·추가 AI 호출 없이 기존 Haiku 분석 1회에서 읽을 가치 정보를 함께 추출한다.

### 범위와 완료 기준
- [x] 기존 Haiku 응답에 `why_read`, `novelty`, `evidence_quality`를 추가하고 값 범위를 검증한다.
- [x] 본문 전문·RSS·제목 기반 분석을 `analysis_depth`로 구분해 과도한 신뢰를 막는다.
- [x] 반도체·시장 시황·관심 종목, 새 촉매, 근거 수준, 소스 신뢰도, 최신성을 조합한 결정적 선별 함수를 작성한다.
- [x] 투자 무관 글·URL 없는 글을 제외하고 가능한 경우 서로 다른 필자의 글 3개를 고른다.
- [x] 기존 `오늘 새로 볼 것`을 `오늘 꼭 읽을 글 3개`로 교체하고 선정 이유·분석 깊이·소스 신뢰도를 표시한다.
- [x] 관심 종목·의견 충돌·종합의견·전체 글 기능은 보존한다.
- [x] 회귀 테스트 107/107, 프로덕션 빌드, 실데이터 데스크톱·모바일 레이아웃과 원문 링크 3개를 검증한다.

### 이번 단계에서 하지 않는 것
- 선호 블로그 학습과 유용/별로/나중에 읽기 피드백은 실제 추천 품질을 먼저 확인한 뒤 Phase 12에서 다룬다.
- GraphRAG·n8n·Gemini·새 데이터베이스를 추가하지 않는다.

## Phase 12 — 반도체 데일리 펄스 + 목적형 3개 편성 (완료)

### 목적
- 반도체 비중이 높은 사용자가 오늘 반도체 시황과 달라진 촉매를 30초 안에 파악한다.
- `오늘 꼭 읽을 글`이 단순 종합점수 상위 3개가 아니라, 가능한 경우 `반도체·시장 시황 1개 + 신선 아이디어 1개 이상 + 추가 핵심 1개`로 편성되게 한다.
- 기존 데이터만 사용해 Claude 호출과 운영비를 추가하지 않는다.

### 범위와 완료 기준
- [x] 최근 2일 반도체 글의 강세·약세·중립, 필자 수, 많이 언급된 종목과 새 촉매를 결정적으로 집계한다.
- [x] 반도체 데이터가 없거나 방향성이 부족한 경우 과장하지 않는 상태 문구를 표시한다.
- [x] 종합의견 아래에 `반도체 데일리 펄스`를 배치하고 종목 클릭·촉매 원문 이동을 제공한다.
- [x] 추천 3개 중 가능한 경우 반도체/시장 시황과 신선한 비관심종목 아이디어를 각각 최소 1개 포함한다.
- [x] 각 추천 카드에 `반도체·시황`, `신선 아이디어`, `추가 핵심` 역할을 표시한다.
- [x] 기존 관심 종목·의견 충돌·소스 적중률·전체 글 기능을 보존한다.
- [x] 회귀 테스트 116/116, 프로덕션 빌드, 실데이터 데스크톱·모바일 375px 화면을 검증했다.

### 이번 단계에서 하지 않는 것
- 실시간 시세·알림·보유 수량·새 모델·새 데이터베이스는 추가하지 않는다.
- `유용/별로/나중에 읽기` 피드백은 이번 두 기능의 실제 사용성을 확인한 뒤 별도 단계로 검토한다.

## Phase 13 — 프리폭스형 개인화 홈 실험 (완료)

### 목적
- 첫 화면을 긴 리서치 보고서에서 `30초 원문 선별형 개인 투자 브리핑`으로 바꾼다.
- 기존 분석 기능은 삭제하지 않고 목적별 탭으로 보존해 되돌릴 수 있게 한다.
- 새 API 호출·새 데이터 소스 없이 현재 JSON만 재배치한다.

### 화면 구조와 완료 기준
- [x] `홈`: 오늘의 종합판단, 전일 대비 새로 부각·전환 주제, 필독 원문 3개, 반도체 데일리 펄스만 우선 표시한다.
- [x] `아이디어`: 기존 상세 종합 리포트, 관심 종목·의견 충돌, Fear & Greed와 종목 관심 추이를 보존한다.
- [x] `소스`: 기존 1년 적중률과 소스별 관련 글을 보존한다.
- [x] `전체 글`: 기존 검색·필터·점진 로딩 목록을 보존한다.
- [x] 데스크톱은 상단 탭, 모바일은 앱형 하단 탭으로 같은 네 가지 동선을 제공한다.
- [x] 소스 관련 글 클릭 시 자동으로 `전체 글` 탭으로 이동하고 해당 필자를 필터링한다.
- [x] 전일 비교 데이터가 없으면 변화가 있다고 추측하지 않고 `비교 데이터 부족`으로 표시한다.
- [x] 회귀 테스트 121/121, 프로덕션 빌드, 실데이터 네 탭과 모바일 375px 검증을 완료했다.

### 이번 단계에서 하지 않는 것
- 포인트·VIP·투표·계좌 연동·GraphRAG는 도입하지 않는다.
- 기존 수집기와 Claude 프롬프트는 변경하지 않는다.

## Phase 14 — 개인 피드백 루프 1단계 (완료)

### 목적
- 추천 원문에 대한 실제 사용자 판단을 기기 안에 기록한다.
- `나중에` 선택한 글을 홈에서 잃어버리지 않고 다시 열 수 있게 한다.
- 적은 표본으로 추천이 왜곡되는 것을 막기 위해 이번 단계에서는 자동 순위 학습을 하지 않는다.

### 범위와 완료 기준
- [x] 추천 카드에 `유용함`, `나중에`, `별로`를 제공하고 같은 선택을 다시 누르면 취소한다.
- [x] 피드백은 API 전송 없이 브라우저 localStorage에만 저장한다.
- [x] 글이 7일 데이터에서 사라져도 `나중에` 글의 제목·출처·원문 링크를 유지한다.
- [x] 홈에서 `나중에 보기` 목록을 확인하고 원문으로 이동할 수 있다.
- [x] 기존 원문 열람 통계·추천 순서·다른 탭을 변경하지 않는다.
- [x] TDD 127/127, 프로덕션 빌드, 데스크톱·모바일 상호작용을 검증한다.

### 이번 단계에서 하지 않는 것
- 서버 동기화와 사용자 계정은 추가하지 않는다.
- 최소 10개 이상의 피드백이 쌓이기 전에는 소스·섹터 가중치를 자동 변경하지 않는다.

## Phase 15 — 오늘 중심 2화면 통합 (완료)

### 목적
- 매일 30~60초 안에 `종합의견 → Peter K Fear & Greed → 독특한 글 → 소스 적중률`을 한 화면에서 확인한다.
- 같은 데이터와 추천 글을 반복하던 홈·아이디어 화면을 합친다.
- 검색·필터가 필요한 전체 글만 별도 화면으로 유지한다.

### 범위와 완료 기준
- [x] 메인 메뉴는 `오늘`, `전체 글` 두 개만 제공한다.
- [x] 오늘 화면 상단에 종합의견과 Peter K Fear & Greed를 함께 배치한다.
- [x] Peter K 지수는 모바일 첫 2회 스크롤 안에 보이고 표본 수·신뢰도를 숨기지 않는다.
- [x] 오늘의 독특한 글 3개와 1년 소스 적중률 상위 5개를 한 화면에서 확인한다.
- [x] 기존 아이디어 상세 분석·반대 의견·종목 추이는 `상세 시황` 펼침 영역으로 보존한다.
- [x] 홈과 아이디어에서 같은 종합의견·추천 글이 중복 출력되지 않는다.
- [x] 전체 글 검색·필터·점진 로딩은 그대로 유지한다.
- [x] TDD 134/134, 프로덕션 빌드, 데스크톱·모바일 상호작용을 검증한다.

### 이번 단계에서 하지 않는 것
- Peter K 2년치 이력 수집·백테스트는 데이터 작업으로 분리한다.
- 수집기, Claude 프롬프트, 추천 점수 계산식은 변경하지 않는다.
- 기존 상세 분석 컴포넌트를 삭제하거나 대규모 리팩터링하지 않는다.

## Phase 16 — Peter K 장기 역발상 지수 검증 (완료)

### 목적
- 최근 7일 한두 글로 정한 BETA 점수를 장기 검증 가능한 지표로 바꾼다.
- 피터케이의 비관적 시장 발언이 실제로 시장 저점과 겹쳤는지 5·20거래일 성과로 확인한다.
- 2년을 기본 범위로 사용하고, 시장 관점 표본이 부족할 때만 최대 3년으로 확장한다.

### 사전 감사 결과
- 기존 `history.json`은 2024-07-01~2026-08-11의 가격·종목 의견을 보유하지만 과거 시장 심리와 근거는 보유하지 않는다.
- 현재 `peter_fear_greed`는 최근 7일 시장 글 1개만 반영해 점수 75, 신뢰도 `매우 낮음`이다.
- 기존 `.scrape-cache.json`은 종목·방향만 저장하므로 장기 시장 심리 백테스트에 직접 사용할 수 없다.

### 범위와 완료 기준
- [x] 공개 글 목록·본문을 수집하고, 최종적으로 2년 전체 764개를 판정해 키워드 선별 편향을 제거했다.
- [x] 피터케이 전용 경량 이력에 날짜·제목·URL·시장 심리·근거를 저장하고 재실행 시 중복 분석하지 않는다.
- [x] 최근 7일 지수는 장기 이력에서 계산하고 표본 수·데이터 범위를 함께 표시한다.
- [x] 극단적 공포/탐욕 이후 KOSPI·KOSDAQ 5·20거래일 수익률과 표본 수를 백테스트한다.
- [x] 2년 시장 관점 표본이 353개로 기준 60개를 충족해 3년 확장은 하지 않았다.
- [x] 과거 데이터가 없거나 가격 매칭이 불가능하면 성과를 추정하지 않고 `대기`로 표시한다.
- [x] 기존 수집·추천·소스 적중률을 보존하고 회귀 테스트 141/141과 프로덕션 빌드를 통과했다.

### 검증 결과
- 범위: 2024-08-13~2026-08-12, 시장 관점 353개.
- 독립 사건: 극단 공포 4회, 극단 탐욕 12회(7일 내 같은 극단은 한 사건으로 묶음).
- 극단 공포 후 평균: KOSPI 5일 -0.7%·20일 -3.3%, KOSDAQ 5일 +3.8%·20일 -5.7%.
- 결론: 극단 공포 표본이 3~4회에 불과해 `비관적 발언=시장 저점` 가설은 아직 입증되지 않았다.

### 이번 단계에서 하지 않는 것
- 결과가 좋아 보이도록 임계값이나 기간을 사후 조정하지 않는다.
- Peter K 지수를 매수·매도 신호로 표시하지 않는다.
- 전체 4,988개 글을 무조건 AI 분석하지 않는다.

## Phase 17 — 투자자 페르소나 기반 원문 판단 카드 (완료)

### 목적
- 대시보드를 단순 요약기가 아니라 `무엇을 읽고 무엇을 추가 검증할지` 알려주는 리서치 비서로 바꾼다.
- 삼성전자·SK하이닉스 중심의 반도체 투자자 관점과 반대 근거·소스 신뢰도를 함께 반영한다.
- 매수·매도 신호는 만들지 않고 원문 열람과 추가 조사 행동만 분류한다.

### 현재 확인된 문제
- 2026-08-12 데이터 132개 중 `why_read`와 유효한 `evidence_quality`가 있는 글이 0개다.
- 추천 카드가 모두 `PICK`이라 반드시 읽을 글과 참고 글을 구분하지 못한다.
- 반대 근거와 삼성전자·SK하이닉스 영향이 카드에 직접 노출되지 않는다.

### 범위와 완료 기준
- [x] AI 응답에 행동 분류, 근거 등급과 이유, 가장 강한 반대 근거, 가격 반영 여부, 투자 논리 연결, 삼성전자·SK하이닉스 영향을 추가한다.
- [x] 누락·잘못된 AI 응답은 허용된 값으로 정규화하고 근거 없는 확신 대신 `판단 불가`로 처리한다.
- [x] 추천 카드의 `PICK`을 `반드시 원문 읽기|추가 조사하기|관심 목록에 저장|기존 투자 논리 점검|참고만 하기|제외하기` 중 하나로 교체한다.
- [x] 낮은 소스 적중률·본문 부족·낮은 근거 등급은 행동 분류에서 과대평가하지 않는다.
- [x] 추천 카드에 가장 강한 반대 근거와 삼성전자·SK하이닉스 영향 경로를 한 줄씩 표시한다.
- [x] 기존 추천 3개, 피드백, 원문 이동, Peter K, 소스 적중률 기능을 보존한다.
- [x] 현재 수집분을 새 스키마로 갱신하고 회귀 테스트·프로덕션 빌드·모바일 화면·Vercel 라이브를 검증한다.

### 검증 결과
- 2026-08-11~12 수집분 29건 모두 행동 분류·근거 등급·반대 근거·투자 논리·관심 종목 영향 필드를 생성했다.
- 행동 분포: 반드시 원문 읽기 10, 추가 조사하기 14, 기존 투자 논리 점검 1, 참고만 하기 2, 제외하기 2.
- 추천 상위 3건은 모두 근거 B등급이며 1년 적중률 46.3~58.6% 소스에서 선별됐다.
- 회귀 테스트 153/153 통과, 프로덕션 빌드와 데스크톱·모바일 화면을 확인했다.

### 이번 단계에서 하지 않는 것
- 매수·매도·목표가를 AI가 결정하지 않는다.
- 사용자 계정·DB·피드백 기기간 동기화는 추가하지 않는다.
- 전체 화면 재설계나 추천 개수 변경은 하지 않는다.

## Phase 18 — 잠실개미 텔레그램 제거·블로그 우선 정책

### 목적
- 잠실개미 블로그(`68083015`)는 유지하고 텔레그램 채널(`jake8lee`)만 수집 대상에서 제거한다.
- 원문 선별 시 동일하거나 비슷한 품질이면 휘발성 텔레그램보다 축적성과 재확인성이 높은 블로그 글을 우선한다.

### 완료 기준
- [x] `config/blogs.json`에 잠실개미 블로그가 유지된다.
- [x] `config/telegram-channels.json`에서 `jake8lee`가 제거된다.
- [x] 현재 7일 데이터에서 `jake8lee` 글이 제거되고 이후 자동 수집에서도 다시 들어오지 않는다.
- [x] 추천 점수가 비슷한 글은 블로그가 먼저 나오고, 종합의견 근거 원문은 동점일 때 블로그를 우선한다.
- [ ] 회귀 테스트와 프로덕션 빌드를 통과하고 Vercel에서 확인한다.

### 보존 원칙
- 과거 적중률 이력은 `person: 잠실개미`로만 저장되어 블로그·텔레그램을 안전하게 분리할 수 없으므로 삭제하지 않는다.
- 텔레그램 전체를 배제하지 않으며, 명확히 더 중요한 정보는 계속 추천될 수 있게 작은 블로그 우선 가중치만 적용한다.
