# Handoff — 네이버 블로그·텔레그램 투자 리서치 대시보드

## 2026-08-17 — v1.0.1 외부 공유 정리

- v1.0.0 기능과 운영 데이터는 유지하고 패키지 버전·README·공개 문서만 정리했다.
- 개인 PC 절대경로를 제거하고 Node.js 20 기반의 복제·실행 방법과 GitHub Actions용 Secret 경계를 명시했다.
- 별도 오픈소스 라이선스는 사용자가 결정할 사항이므로 임의로 추가하지 않았다.
- 기능 코드·Peter K BETA·수집 데이터·n8nversion은 변경하지 않았다.

## 2026-08-17 — v1.0.0 정식 동결

- 4역할 리서치팀을 실제 GitHub Actions 데이터와 Vercel 화면까지 배포했다. 첫 화면에서 최부장 종합판단을 확인할 수 있다.
- 2년 백필 실행 `31929221122`가 성공했다. 범위 2024-08-16~2026-08-16, 원문 54,752건, 분석 단위 11,580건, 실패 0건이다.
- 공식 방향성 의견 12,178건을 7일 반복 제거 후 5,477개 독립 에피소드로 계산했고 21개 소스의 3개월·1년 통계를 생성했다.
- 최종 데이터 커밋은 `c5ac475`, 이후 2026-08-17 자동 수집 커밋 `03c2e88`까지 동기화했다.
- AI JSON 복구(`bebe5a4`), 부분 데이터 배포 차단(`39ba2b8`), 일시적 원문 수집 오류 재시도(`0f46bba`)를 적용했다.
- 회귀 테스트 216/216, Vite 프로덕션 빌드 55 modules, 라이브 화면의 최부장 종합판단과 소스 통계를 검증했다.
- `v1.0.0`은 코드·문서 기준선이다. 예약 수집에 따른 JSON 데이터 커밋과 Vercel 자동 배포는 계속된다.
- 이후 기능 변경은 `plan.md`에 새 단계를 만들고 실패 테스트를 먼저 작성한다. `.npm-cache/`와 `n8nversion`은 계속 제외한다.

## 2026-08-15 세션 33 — 4역할 리서치팀 로컬 구현(미배포)
- 운영 안정 버전 위에 `김사원 → 이대리·박과장 → 최부장` 리서치 조직을 최소 변경으로 추가했다. n8n·Supabase·별도 DB는 도입하지 않았다.
- 김사원은 기존 개별 글 분석에서 근거 A~F, 출처 역할, URL, 수치와 데이터 공백을 결정적으로 정리해 추가 AI 비용이 없다.
- 이대리는 직접 의견만 인물 기준으로 비교하고, 박과장은 가격·지수·수급과 독립 대조한다. 두 Haiku 호출은 병렬이며 서로의 결과를 보지 않는다.
- 최부장 Opus는 세 보고서와 원자료 요약을 받아 기존 `headline/positive/negative/minority/events` 호환 필드와 `decision/confidence/summary/reasons/counter_case/watch_items/invalidation_conditions`를 생성한다.
- `RESEARCH_TEAM_VERSION`을 브리핑 입력 해시에 포함해 프롬프트·역할 버전 변경 시 기존 캐시를 한 번만 무효화한다.
- 첫 화면은 `최부장 종합판단`과 확신도를 보여주고, 접힌 상세 시황에서 네 역할의 보고서와 원문 근거를 확인한다. 과거 리포트는 기존 UI로 계속 열린다.
- TDD 203/203, Vite 빌드 55 modules, 데스크톱 1440px·모바일 375px 가로 넘침 0·콘솔 오류 0을 확인했다.
- 실제 Claude API 실행, 데이터 갱신, 커밋, push, Vercel 배포는 하지 않았다. 다음 단계는 사용자 승인 후 실제 1회 수집으로 역할별 JSON 품질·실행시간·비용을 확인하는 것이다.

## 마지막 업데이트
2026-08-17 (v1.0.1, Codex)

### 세션 32 — 문서 최신화·GitHub 버전 경계 확정
- 이 저장소를 **GitHub 버전 또는 Vercel 버전**으로 명명했다. 실제 구조는 GitHub Actions가 수집·분석하고 Vercel이 React 화면을 배포한다.
- 별도 n8n 실험은 형제 경로 `..\n8nversion`에서 진행한다. 명시적 요청 없이 이 저장소와 파일·Secrets·자동화를 섞지 않는다.
- 원격 최신 커밋 `478b79d`까지 fast-forward했다. 로컬 `posts.json` 기준일은 2026-08-14이며 최근 7일 글 126건(블로그 52·텔레그램 74), 브리핑 7일분이다.
- 실제 설정은 블로그 15개 + 텔레그램 7개이며, 자동 실행은 매일 KST 08:00과 평일 KST 16:30이다. 오래된 문서의 10개·일 1회 표기를 수정했다.
- README의 Vite 기본 문서를 실제 제품·실행·보안·문서 안내로 교체하고 CLAUDE.md·PRD.md·SKILL.md·plan.md·PROGRESS.md를 현재 구조에 맞췄다.
- 로컬 회귀 테스트 **193/193**, Vite 프로덕션 빌드 성공(54 modules)을 재확인했다. 이 세션은 문서만 수정했으며 애플리케이션 코드와 배포 설정은 변경하지 않았다.
- 새 대화는 이 경로를 작업 폴더로 선택하고 `PROGRESS.md → CLAUDE.md → PRD.md → plan.md → 필요한 handoff.md 구간` 순서로 읽는다.

### 세션 31 — 독립 감사 P0 반영
- 종합판단 캐시를 날짜 단위가 아닌 글·가격·시장 데이터 입력 해시 단위로 변경했다.
- 첫 화면을 `종합판단 + Peter K → 오늘 꼭 읽을 글 → 시장 객관 데이터 → 소스 실험 통계` 순서로 조정했다.
- 분석 스키마 v3의 직접 의견만 신규 독립 의견 수와 소스 실험 통계에 포함한다.
- 회귀 테스트 193/193, GitHub Actions #105, Vercel 데스크톱·모바일 화면을 검증했다. 자세한 기술 기록은 문서 하단의 세션 31 기록을 참조한다.

### 세션 30 — 신규 소스·중복 제거·시장 팩트 경계
- 블로그 4개를 추가했다: 이그전(`egzion`), KK Kontemporaries(`kk_kontemp`), 레드버드(`redbirdstock`), Granit34(`granit34`). 각 RSS가 HTTP 200·50건을 반환하는 것을 사전 확인했다.
- 미국 기술주·반도체 보완용 `itechkorea` 텔레그램만 2026-08-26까지 시험 운영한다. `activeTelegramChannels()`가 2026-08-27부터 자동 제외하므로 수동 삭제에 의존하지 않는다.
- 중복 운영자 채널은 추가하지 않았다: `kkkontemp`↔`kk_kontemp`, `redbirdstock` 텔레그램↔동명 블로그, `Joorini34`↔`granit34`. `defence_24`, `ym_research`는 전달형 자료와 중복량이 많아 보류했다.
- 분석 스키마에 `source_role: opinion|fact|mixed`를 추가했다. 직접 의견인 `opinion`만 신규 적중률 이력에 포함하며, 전달 사실 `fact`와 직접 의견을 분리하기 어려운 `mixed`는 제외한다. 기존 이력은 출처 역할을 소급 판정할 수 없어 보존한다.
- 스탁이지/인텔리오에서 참고할 수 있는 핵심인 ‘시장 정량 사실과 필자 의견 분리’를 구현했다. 기존 공개 JSON의 KOSPI·KOSDAQ 1·5·20일 변화와 외국인 수급을 `시장 정량 사실` 보드로 보여준다. 로그인·유료·비공개 데이터나 근거 없는 RS/MTT는 사용하지 않는다.
- 첫 실제 실행 #99에서 31건 순차 분석이 10분 제한에 걸리는 운영 병목을 확인했다. Actions 제한을 20분으로 늘리고, `analysis_version`이 현재 스키마와 같은 URL은 Claude 결과를 재사용하도록 바꿨다. 프롬프트가 바뀌면 버전을 올려 한 번만 갱신하므로 이후 실행 시간과 토큰을 함께 줄인다.
- #100에서 구버전 31건을 13분 24초에 1회 마이그레이션했고, 바로 이어진 #101은 분석 재사용으로 55초(수집·요약 단계 25초)에 완료됐다. 원격 JSON 134건 중 신규 소스는 이그전 1건·ITK 2건이며 `source_role`과 `analysis_version: 2` 저장을 확인했다.
- 신규 API 키는 없다. 기존 `CLAUDE_API_KEY`만 계속 사용한다. 회귀 테스트 172/172, 프로덕션 빌드 성공, 모바일 가로 넘침 0, Vercel에서 신규 글과 시장 팩트 보드를 확인했다.

### 세션 29 — 잠실개미 텔레그램 제거·블로그 우선
- 잠실개미 블로그(`68083015`)는 유지하고 텔레그램 채널 `jake8lee`만 수집 설정에서 제거했다. 현재 7일 데이터에서도 해당 채널 글 14건을 제거해 즉시 화면에서 빠지게 했다.
- `오늘 꼭 읽을 글`에는 블로그 +8점의 작은 가중치를 추가했다. 관심 종목·촉매·근거 품질보다 낮은 값이므로, 텔레그램 글이 명확히 더 중요하면 계속 추천될 수 있다.
- 종합의견의 근거 원문은 주제 일치도가 같은 경우 블로그를 우선한다. 텔레그램 전체를 배제하지는 않는다.
- 장기 적중률 이력은 잠실개미 블로그와 텔레그램을 구분할 출처 필드가 없어 삭제하지 않았다. 잘못된 대량 삭제를 피하고 과거 성과 기록을 보존했다.
- TDD 158/158 통과, Vite 프로덕션 빌드 성공. 새 소스 추가는 사용자가 제안할 채널을 별도 검토한 뒤 진행한다.

### 세션 27 — Peter K 2년 역발상 지수 검증
- 피터케이 공개 글 최근 2년 764개를 수집했다. 키워드 선별이 은유적 시장 글을 누락하는 편향을 발견해 최종적으로 전체 글을 Claude로 판정했다.
- `public/data/peter-history.json`에 시장 관점 353개(2024-08-13~2026-08-12)를 경량 저장했다. 기준 60개를 충분히 넘어 사용자 규칙에 따라 3년 확장은 하지 않았다.
- 같은 극단이 7일 내 반복되면 한 사건으로 묶고, 게시일 다음 거래일 종가부터 KOSPI·KOSDAQ 5·20거래일 수익률을 계산한다.
- 독립 사건은 공포 4회·탐욕 12회다. 공포 뒤 평균은 KOSPI 5일 -0.7%·20일 -3.3%, KOSDAQ 5일 +3.8%·20일 -5.7%다. 표본이 작아 `비관=저점` 가설은 아직 입증되지 않았음을 UI에 표시한다.
- 일일 수집기는 새 피터케이 시장 글만 장기 이력에 증분 병합하고 GitHub Actions가 함께 커밋한다. 과거 전체 분석은 반복하지 않는다.
- TDD 141/141, Vite 프로덕션 빌드 성공. 데스크톱·모바일 375×812에서 장기 검증·접힘·가로 넘침 없음 확인.

### 세션 26 — 오늘 중심 2화면 통합
- 안드레 카파시 스킬의 단순성·최소 변경 원칙으로 메인 메뉴를 `오늘 / 전체 글` 두 개로 축소했다. 홈과 아이디어가 같은 `daily_briefs`와 추천 글을 반복하던 구조를 제거했다.
- 오늘 화면 상단을 `종합의견 + Peter K Fear & Greed` 2단으로 구성했다. 모바일에서는 종합의견 바로 다음에 Peter K 카드가 나타나며, 현재 표본 1개·신뢰도 매우 낮음·2년 백테스트 전이라는 한계를 그대로 표시한다.
- `오늘의 독특한 글` 3개와 1년 소스 적중률 상위 5개를 같은 화면에 배치했다. 전체 소스 성적표는 접힘 영역으로 보존하고 소스 선택 시 해당 필자의 전체 글로 이동한다.
- 기존 종합 리포트·어제와 달라진 것·반도체 펄스·종목 관심 추이는 삭제하지 않고 `상세 시황·반대 의견 보기` 안에 기본 접힘 상태로 보존했다.
- 수집기·Claude 프롬프트·추천 알고리즘·Peter K 계산식은 변경하지 않았다. Peter K 2년치 이력 수집과 5일·20일 백테스트는 다음 데이터 단계로 분리했다.
- TDD **134/134**, Vite 프로덕션 빌드 성공. 데스크톱·모바일 375×812에서 메뉴 2개, Peter K 상단 노출, 상세 펼침, 소스→전체 글 이동, 가로 넘침 없음, 앱 콘솔 오류 0건을 확인했다.

---

### 세션 25 — 개인 피드백 루프 1단계
- 홈의 `오늘 꼭 읽을 글` 3개에 `유용함 / 나중에 / 별로` 선택을 추가했다. 같은 선택을 다시 누르면 해제되고, 다른 선택을 누르면 기존 상태를 교체한다.
- 피드백은 `dashboard:feedback:v1` localStorage에만 저장한다. 서버·Claude API 호출이 없어 토큰 비용이 들지 않으며, 기기 간 동기화는 아직 하지 않는다.
- `나중에` 글은 제목·출처·원문 URL 스냅샷을 함께 저장해 7일 데이터에서 사라져도 홈의 `나중에 보기`에서 유지한다.
- 수집 실행마다 글 id가 달라질 수 있어 피드백 키는 id가 아닌 안정적인 원문 URL을 우선 사용한다. 실제 새로고침 복원 불일치를 발견해 이 방식으로 보정했다.
- 자동 추천 가중치 반영은 과적합 방지를 위해 보류했다. 최소 10개 피드백이 쌓인 뒤 소스·섹터 선호를 검토한다.
- TDD **127/127**, Vite 프로덕션 빌드 성공. 데스크톱에서 저장·교체·해제·새로고침 유지, 모바일 375×812에서 버튼 폭·단일 열 목록·하단 메뉴·가로 넘침 없음·콘솔 오류 0건을 검증했다.

---

### 세션 24 — 프리폭스형 개인화 홈
- 사용자가 대시보드를 잘 열지 않는 원인을 `정보 부족`보다 `첫 화면의 정보 위계와 반복 동선 부족`으로 판단해, 기존 기능을 삭제하지 않고 홈 실험을 추가했다.
- 첫 화면을 `오늘의 종합판단 → 오늘 꼭 읽을 글 3개 → 어제와 달라진 것 → 반도체 데일리 펄스` 순으로 재배치. 원문 선별이 펄스 아래로 밀리지 않도록 실화면 검증 후 순서를 한 번 보정했다.
- `src/utils/personal-home.js` 신설: 최신 종합 리포트에서 주도·리스크·소수의견을 압축하고 전일 대비 방향 전환과 새로 부각된 주제를 결정적으로 계산한다. 전일 자료가 없거나 차이가 없을 때 과장하지 않는다.
- 목적별 네 탭 도입: `홈`, `아이디어`(기존 상세 종합 분석·관심종목·충돌·Fear & Greed), `소스`(1년 적중률), `전체 글`(검색·필터·점진 로딩). 데스크톱은 상단, 모바일은 고정 하단 내비게이션을 사용한다.
- 새 API·Claude 호출·데이터 소스는 추가하지 않았다. HOME1~HOME5 테스트 추가, 전체 **121/121** 통과, Vite 빌드 성공(53 modules). 실데이터 네 탭 전환, 전체 글 검색, 모바일 375px 하단 메뉴, 가로 넘침 없음, 콘솔 오류 0건을 확인했다.

---

### 세션 23 — 반도체 데일리 펄스 + 목적형 3개 편성
- 사용 목적을 `반도체 시황 파악 + 매일 신선한 투자 아이디어 선별`로 재확인하고, 비용·복잡도를 늘리지 않는 두 기능을 우선 구현했다.
- `반도체 데일리 펄스` 신설: 최근 2일 반도체 관련 글의 강세·약세·중립, 필자 수, 시장 관점 수, 자주 언급된 종목, 새 촉매와 원문을 압축 표시한다. 데이터가 없으면 `데이터 부족`, 방향성 의견이 없으면 `방향성 부족`으로 과장 없이 표시한다.
- `오늘 꼭 읽을 글`을 목적형으로 편성: 가능한 경우 반도체 글을 일반 시황보다 우선하고, `반도체·시황` 1개와 비관심 종목의 `신선 아이디어` 1개를 먼저 확보한 뒤 `추가 핵심`으로 채운다. 후보가 없으면 억지로 만들지 않는다.
- TDD: MR8~MR9, SP1~SP7 추가. 전체 **116/116** 통과, Vite 프로덕션 빌드 성공(51 modules). 실데이터에서 반도체 15건·9명, 강세 11/중립 3/약세 1과 촉매 원문 3개를 확인했다.
- Chrome 실화면 검증: 역할 라벨 3개, 종목 버튼, 촉매 링크를 확인했다. 모바일 375px에서 문서 가로 넘침 없음, 펄스 1열 전환과 추천 카드 폭 정상. 콘솔의 메시지 채널 오류 2건은 Chrome 확장 연결 과정에서 발생했으며 앱 렌더링 오류는 없었다.

---

### 세션 22 — 오늘 꼭 읽을 글 3개
- 사용자 사용 목적을 **반도체 중심 시황 파악 + 매일 신선한 투자 아이디어 원문 선별**로 재확정.
- 기존 `오늘 새로 볼 것 1+2+1`을 프리폭스형 개인화 홈 구조를 참고한 `오늘 꼭 읽을 글 3개`로 교체. 1위 큰 카드 + 2·3위 보조 카드, 명확한 원문 버튼을 제공하되 기존 증권사 리포트 톤 유지.
- 추가 AI 호출 없이 기존 Haiku 응답에 `why_read`, `novelty`, `evidence_quality`를 추가하고 수집 단계에서 `analysis_depth`(본문/RSS/제목)를 기록.
- `src/utils/must-read.js` 신설: 반도체·시장 시황·삼성전자/SK하이닉스·새 촉매·근거 수준·본문 깊이·1년 소스 신뢰도를 조합해 최근 2일 원문 최대 3개 선별. 투자 무관·URL 없음·중복 URL 제외, 가능한 경우 필자 다양성 확보.
- 제목 기반 분석은 AI가 높은 근거 점수를 반환해도 근거 품질 0으로 처리해 과신 방지. 구 데이터는 `분석 범위 미기록`으로 정직하게 표시하며 촉매·근거·요약으로 선정 이유 폴백.
- 검증: 회귀 107/107, 프로덕션 빌드 성공, 실데이터 원문 링크 3개, 데스크톱 가로 넘침 없음, 모바일 360px 가로 넘침 없음 확인.

---

2026-08-07 (세션 16, Codex)

> ⚠️ 이 문서는 다음 세션(더 작은 모델 포함)이 그대로 이어받을 수 있게 작성됨.
> 규칙: **CLAUDE.md 필독** → 기능 만들기 전 필요성 확인 / 검증→구조→구현 / 완료=라이브 확인.

---

## 1. 현재 상태: 전체 완성, 라이브 운영 중 ✅

- **URL**: https://naver-blog-dashboard.vercel.app
- **자동화**: GitHub Actions 매일 KST 08:00 (`.github/workflows/collect.yml`) → 수집→분석→push→Vercel 자동배포
- **소스 17개**: 블로그 11 (`config/blogs.json`) + 텔레그램 6 (`config/telegram-channels.json`). 2026-08-12 잠실개미 텔레그램 제거, 잠실개미 블로그 유지. 과거 데이터와 적중 기록은 보존.
- **모델 배분(의도된 설계, 바꾸지 말 것)**: 개별 글 분석 = `claude-haiku-4-5-20251001` / 종합 리포트 = `claude-opus-4-8` (하루 1회)

## 2. 대시보드 구성 (위→아래)

| 섹션 | 컴포넌트 | 내용 |
|---|---|---|
| 🎯 소스 적중률 | `SourceScores.jsx` | **최상단 우선 배치**. 표본 보정 종합 신뢰도·1년·3개월 순위, 상위 5개 소스, 최근 7일 관련글(소스별 최대 2개), 소스 클릭 필터 |
| 📊 종합 리포트 | `DailyBrief.jsx` | 결론→요약→📈강세/📉약세 논거→🔍소수·역발상→⚖️말vs가격→🎯관전포인트→종목 |
| 😨 피터케이 Fear & Greed BETA | `PeterFearGreed.jsx` | 피터케이의 시장 관점 글을 최근 심리 점수로 표시. 2년 백테스트 전 실험 지표이며 매매 신호 아님 |
| 🔎 종목 관심 추이 | `AttentionTrends.jsx` | 7일 언급 인원 + 최근2일vs이전2일 변화(🆕신규/▲급증/▼둔화) + 주가 1일/5일 + 🔀의견갈림 + 시각전환. **행 클릭→종목 리포트** |
| 📄 종목 리포트(모달) | `StockReport.jsx` | 현재가+등락, 종목별 모든 소스의 강세/약세 시각·수치·리스크·원문링크 |
| 개별 글 | `PostCard/FilterBar/PostModal/StatsBar` | 날짜/소스/섹터/블로그 필터 + 카드(리스크 노출) |

## 3. 데이터 파이프라인 (`scripts/collect-rss.js` 단일 파일)

```
블로그 RSS + 텔레그램(t.me/s/ 스크래핑) 수집 (오늘+어제 2일)
→ Haiku 개별 분석 (headline/summary/stocks/sector/stance/reasoning/risks/numbers)
→ 종목 별칭 정규화 (config/stock-aliases.json)
→ 주가 수집: 7일 내 2명+ 언급 종목(상한 25) — 네이버 자동완성(이름→코드, config/stock-codes.json 캐시)
   + 국내 siseJson / 해외 api.stock.naver.com → d1/d5/d20 등락률
→ 시황 수집: KOSPI/KOSDAQ 지수 + finance.naver.com investorDealTrendDay 수급(개인/외인/기관, 외인5일 누적)
→ Opus 종합 리포트 (여론+주가+시황 전부 주입 → 말vs가격 괴리 분석 포함)
→ public/data/posts.json 저장 { date, daily_briefs[7], market, prices, posts[7일치] }
→ git push → Vercel
```

핵심 구현 디테일 (다음 세션이 알아야 할 것):
- **person 태그**: 같은 사람이 블로그+텔레그램 양쪽에 있을 수 있음(현재 잠실개미) → 모든 인원 집계는 `person` 기준 중복 제거
- **parseJSONLoose()**: 모델이 JSON 뒤에 텍스트 붙여도 파싱 (분석실패 0건 유지 장치)
- **브리핑 캐시**: daily_briefs[0].date == 오늘이면 재생성 안 함. 브리핑 재생성 테스트 시 오늘 브리핑을 배열에서 제거 후 실행
- 시세/시황 실패해도 여론 파이프라인은 무손상 (try/catch 분리)

## 4. 판정 시스템 (구현 완료 ✅ — 2026-07-14 커밋 85954fc, 5441f4f)

- `scripts/judge.js`: 스펙(judge_verdict + critic) 순수 JS 구현 — AI 호출 없음, 결정적, 비용 0
- 파이프라인 연동: collect-rss.js → judgeBatch() → posts.json에 `verdicts` + `verdict_history`(5회, critic C2용) 저장
- UI: `AttentionTrends.jsx`에서 ILLUSION=True 종목에 🔀괴리 표시 (판정 배지는 안 B 채택으로 제거 — 매매추천 오해 방지)
- 회귀테스트: judge 골든 7케이스 + critic 골든 6케이스 포함 (총 31케이스, npm test)
- 스펙 원문은 `specs/` 유지 (결정표 변경 시 스펙 → 테스트 → 코드 순으로 수정)

## 5. 로컬 실행 방법

```bash
cd naver-blog-dashboard
# API 키는 .env의 CLAUDE_API_KEY (dotenv 미사용 — 수동 주입 필요)
export CLAUDE_API_KEY="$(grep '^CLAUDE_API_KEY=' .env | cut -d= -f2- | tr -d '\r\"')"
node scripts/collect-rss.js   # 전체 파이프라인 (~3-5분)
npm run build                  # 빌드 확인
```
배포 = git add/commit → `git pull --rebase origin main` → push (Vercel 자동)

## 6. 소스 추가 방법

- 텔레그램: `https://t.me/s/{핸들}` 접속되면 공개=수집가능. `config/telegram-channels.json`에 `{id, name, person}` 추가
- 블로그: `config/blogs.json`에 `{id, name, person}` (RSS: rss.blog.naver.com/{id}.xml 확인)
- **적정선 15~20개 (현재 18)**. 추가 시 기존 소스와 중복도·반대시각·타섹터 여부 검증할 것. 25개 초과 시 브리핑 max_tokens 상향 필요

## 7. 알려진 이슈 / 주의

- git `index.lock` 충돌 잦음 → `del .git\index.lock` 후 재시도 (`unlock_and_commit.bat`)
- Actions 워크플로우는 `posts.json` + `config/stock-codes.json` 두 파일을 커밋함 (stock-codes 누락 시 rebase 실패 — 2026-07-12 수정됨)
- 터미널에서 python 한글 출력 시 cp949 인코딩 깨짐 → 파일로 저장 후 Read로 확인
- 네이버 API들은 비공식 — 구조 변경 시 파싱 수정 필요 (시세: siseJson 정규식, 수급: `class="date2"` 정규식)

## 7-B. 장애 대응 런북 (새벽에 파이프라인이 죽었을 때 — AI에게 묻기 전에)

**1단계: 어디서 죽었나** — github.com/Dremandream/naver-blog-dashboard → Actions → 실패한 run 클릭
| 실패한 스텝 | 원인 후보 | 첫 조치 |
|---|---|---|
| 회귀 테스트 | 파싱 코드가 깨짐 (직전 커밋 의심) | `npm test` 로컬 실행 → 실패 케이스명 확인 → 직전 커밋 diff |
| 수집 및 요약 | ①네이버/텔레그램 구조 변경 ②API 키 만료 ③특정 소스 장애 | 로그에서 `❌`/`실패` 검색. "HTTP 4xx"=구조·차단, "API"=키 확인 |
| 커밋 & 푸시 | rebase 충돌 | 로그 그대로 두고 다음 실행에서 자가 복구되는지 확인 (-X theirs 재시도 있음) |
**2단계: 화면 증상으로 역추적** — UI에 "⚠️ N시간 전 데이터" 배너 = 수집이 안 돈 것. 브리핑 날짜만 옛날 = 브리핑 생성만 실패(로그에서 "브리핑 생성 실패" 검색, 보통 토큰 잘림).
**3단계: 로컬 재현** — §5 로컬 실행 → 같은 에러 나면 코드 문제, 안 나면 Actions 환경(Secrets) 문제.
- 실패 시 텔레그램 자동 알림 있음 (TELEGRAM_BOT_TOKEN/CHAT_ID Secrets 등록 시 활성)

## 7-C. 자격증명 규칙
- `.env`는 gitignore됨(확인 완료), 히스토리 유출 없음(스캔 완료). **키를 채팅/터미널에 출력 금지.**
- 2026-07-11 세션에서 CLAUDE_API_KEY가 로컬 터미널에 1회 출력된 이력 있음. 로테이션 권고했으나 **사용자 결정(2026-07-17): 기존 키 유지** — 로컬 노출 1회뿐이고 git 유출 없음. 더 이상 챙기지 말 것. 단, console.anthropic.com Usage에서 이상 사용량 발견 시 그때 Revoke→재발급(.env + GitHub Secrets 교체).

## 8. 다음 로드맵 (우선순위, 구현 전 필요성 재확인)

1. [x] ~~판정 시스템 구현~~ — 완료 (§4 참조)
2. [x] ~~지난 리포트 열람~~ — 완료 (날짜 칩, 세션 10)
3. [x] ~~촉매 캘린더~~ — 완료 (세션 12): 브리핑 신 스키마에 `events[{date,label,stocks,source,approx}]` 추가(Opus가 날짜 명시된 미래 이벤트만 추출, 최대 6개) → `EventCalendar.jsx`가 7일치 병합·중복제거·오늘 이후만 표시, FactSidebar 하단 "📅 주요 일정" 섹션. 커밋 3b649bd
4. [x] **소스 적중률** — Phase 1·2·3 전부 완료(세션 12). **스펙**: 적중=지수 대비 초과수익(KR=코스피, US=나스닥), 5·20거래일. 파이프라인: `history.json`에 날짜별 종가·지수·의견 누적(`archiveHistory`, fetchClosesDated로 의견 8일 구간 자동 백필) → `hitrate.js`의 `computeSourceScores`가 person별 {hits,total,rate}×{5,20} 계산 → posts.json `source_scores` 저장 → UI `SourceScores.jsx`(AttentionTrends 아래). US 지수는 `fetchForeignIndexClosesDated`(.IXIC 나스닥·.INX S&P500) 신규 수집. Actions가 history.json 커밋. 회귀 38건(HR1~7 포함). **소급 백필 2단계 완료(세션 12)**:
- ①git 백필 `scripts/backfill-history.js`: posts.json 커밋 이력에서 과거 의견 복원(방향성 stance는 07-06부터). person 정규화(config 기준, luy1978→피터케이 등).
- ②**3개월 블로그 스크레이핑 `scripts/scrape-history.js`**: PostTitleListAsync 페이지네이션 → 모바일 PostView `se-text-paragraph` 본문 추출 → Haiku 재분석(stocks·stance) → history 병합 → 적중률 재계산. logNo 캐시(.scrape-cache.json, gitignore). 시세 함수에 daysBack 파라미터 추가.
- ③**텔레그램 3개월 스크레이핑 `scripts/scrape-telegram-history.js`**: t.me/s/{채널}?before= 페이지네이션 → 채널×일자 병합 → Haiku 분석 → history 병합. 캐시 .tg-scrape-cache.json. MAX_PAGES=80.
- 결과(라이브, 16소스 중 15명 5일 적중률): 에테르 61.7%(47)·잠실개미 60.4%(154)·IT는SK 47.3%(148)·의교창 47.6%(124)·너는무한 55.2%(145)·투자콤 40%(80) 등 **통계적으로 의미있는 표본**(93일, 2421 의견).
- **검증 완료(세션 12, karpathy-guidelines)**: hitrate.js를 독립 재계산과 교차검증→0 불일치(로직 정확). 데이터 필드 결손 0, person 중복 0, 회귀 38/38, 빌드·UI 통과.
- **알려진 커버리지 갭(silent cap 아님, 명시)**: ⓐ김찰저=t.me/s 302(웹프리뷰 비공개)라 과거 스크레이핑 불가→최근 데이터만. ⓑ고빈도 텔레(펭미업 06-01·에테르 05-20·캬오 04-28·너쟁이 04-30)는 MAX_PAGES=80에 걸려 04-16까지 못 감→부분 커버(표본은 충분). 전체 확보하려면 MAX_PAGES 상향 후 재실행.
- 1년 확장: scrape-history.js/scrape-telegram-history.js에 날짜 인자(예 2025-07-17)만 주면 가능(분석량↑).
- UI 문구 개선: 역행→'엇갈림', 소스적중률 평이한 문구+예시(세션 12). archiveHistory 덮어쓰기→병합. 상세 plan.md.
5. [ ] UI: 요약↔전체 토글, 다크모드 (선택)

## 9. 히스토리 요약

**세션 9~10 전반**: 텔레그램 8채널 통합 → 종합의견을 Opus 증권사 리포트 형식으로(강세/약세/소수의견/관전포인트) → person 중복제거·종목 정규화·JSON파서 등 신뢰도 → UI 리포트 톤 정리 + 중복 패널 통합(스파이크/주간트렌드/핵심종목→관심추이 1개) → **Phase 1: 주가 병기(말vs가격)** → **Phase 2: 시황 레이어(지수+수급)** → 판정 시스템 스펙 작성. 문서: PRD.md(SSOT), specs/, 구버전 cowork-prompt.md는 ../archive/로.

**세션 10 후반 (2026-07-13~14)**:
- 지난 리포트 열람(날짜 칩) · 쏠림 경고 + 소외된 시각(다양성 필터, 정보과잉 연구 근거)
- 소스 개수 검증: 18개=적정(역U자 연구), 늘리기보다 "반대시각/타섹터"만 선별 추가 원칙
- **증권사 인쇄물 디자인 시스템**: :root 토큰, Noto Serif KR, 종이 시트, 괘선, 제호 "데일리 투자 리포트" (스크린샷 피드백 반영)
- 07-13 폭락일 실전 대응: 지수 하루 지연 원인 규명 → **평일 16:30 오후 크론 추가**, 지수 기준일(asOf) 표기, 브리핑 잘림(글 23개↑) → **max_tokens 5000+재시도**
- **운영 규율 이식**: 회귀테스트 18케이스(npm test, Actions 게이트), 신선도 26h UI 경고, PROGRESS.md(4문서 체계), Actions 실패 텔레그램 알림, 브리핑 스키마 정규화, 장애 런북, 자격증명 점검(히스토리 유출 無, 키 로테이션 권장)
- judge_verdict 스펙을 오늘 실데이터로 시뮬레이션: 폭락일 buy 0·needs_review 2(착시괴리) — 스펙 타당성 확인 → 이후 정식 구현 완료 (§4)

**세션 11 (2026-07-17)**:
- 프로젝트 폴더 정리: 구버전 handoff/plan/중복 Fable5 폴더 삭제 → handoff는 이 파일 하나만 유지
- 판정 시스템 구현 완료 확인 (커밋 85954fc·5441f4f, 라이브 데이터에 verdicts 9종목 정상 작동, npm test 31/31 통과) → handoff §4·로드맵 현행화
- **종합 리포트 간결화**: 스키마 교체 `{headline, positive[{sector,items[{name,point,mentions}]}], negative[...], minority(≤2)}` — 긍정/부정 2단 × 산업별 그룹 × 종목당 근거 1줄. brief 문단·말vs가격(관심추이와 중복)·관전포인트·쏠림·hot_stocks 제거. 구 스키마 리포트(7일치)는 DailyBrief.jsx에서 기존 레이아웃 폴백 렌더. 텔레그램 알림도 신 스키마로.
- 소스 정리: 혀니루 삭제(6개월 휴면) → 17개. 전 소스 반도체 편중 확인 — 추가는 타섹터만 (커밋 2f09064 이전)
- **증권사 리포트 디자인 이식** (D:\산업스터디 PDF들 — NH·메리츠·삼성·DAOL 구조 학습): ①크림슨 아이덴티티 밴드+Daily Note 라벨 ②`FactSidebar.jsx` 신설 — 본문(주장) 좌 2/3 + 시황·역행감지(데이터) 우 1/3, MarketStrip 대체(파일은 남아있으나 미사용) ③소수의견 → headline 아래 Summary 음영박스 ④모바일 `.main min-width:0` + 관심추이 자체 스크롤. 라이브 확인 완료. 커밋 2f09064
- 참고: 디자인 기준 PDF는 D:\산업스터디\ 아래 (design/ 폴더 복사는 사용자 미결정)

**세션 12 (2026-07-17)**:
- **촉매 캘린더 구현** (로드맵 3번, 커밋 3b649bd): 원안 "watch_points 활용"은 불가(신 스키마에서 제거됨 + 구 데이터도 날짜 없는 자유문장) → 브리핑 스키마에 `events` 필드 신설. 프롬프트 규칙 10번: 날짜 특정 가능한 미래 이벤트만, "7월 말"→말일+approx:true, 최대 6개. 정규화(YYYY-MM-DD 검증) 포함. UI: `EventCalendar.jsx`(mergeEvents 내보냄) → FactSidebar 하단. 이벤트 0건이면 섹션 숨김. npm test 31/31, 라이브 확인 완료(7/31 빅테크 실적 이벤트 표시).
- 주의: events는 7/17 이후 브리핑에만 존재 — 축적되면서 캘린더가 채워지는 구조.

**세션 13 (2026-07-21)**:
- **소스 적중률 창 확장** (커밋 0162f28): 5·20일 → **1개월·3개월·1년**(거래일 21·63·252). `hitrate.js`에 `windows` 인자 추가하고 소스 구조를 `w5/w20` → `w[N]`(N=거래일 수)로 일반화, `windows:[{n,label}]` 메타 반환. `SourceScores.jsx`·`App.css`는 windows 메타 기반 동적 컬럼 렌더로 교체. `collect-rss.js` 로그의 `w5.total` 참조도 첫 창 기준으로 수정. **1년(252거래일)은 history가 아직 94거래일뿐이라 전부 "결과 대기"** — 데이터 쌓이면(2027-04경) 자동 표시(정직 표기, silent cap 아님). 1개월은 표본 충분, 3개월은 일부만 성숙. 테스트: 골든 데이터(6거래일)는 `computeSourceScores(hist,[5,20])`로 검증 유지, HR8(windows 메타)·HR9(기본창 [21,63,252]) 추가 → 40/40. 라이브 확인 완료.
- **김찰저 제거**: §1 참조. 재검토 결과 코드 버그 아님 — kimcharger 채널이 t.me/s 웹프리뷰를 꺼서(HTTP 200이지만 메시지 0개, 07-14 이후 미수집) 표본 부족으로 "결과 대기"였던 것. 사용자 지시로 채널 제거. 창 확장과 상충(긴 창일수록 최근 소스 늦게 표시)했으나 제거로 해소됨.
**세션 15 (2026-07-23)**:
- **2년치 백필 완료** (커밋 9223c61): 사용자 판단 = "장기 데이터 없으면 채널 신뢰성(실력 vs 운) 구분 불가" → 1년 적중률(252거래일 보유)을 지금 폭넓게 보려면 2년치 필요(1년표본×1년보유). `scrape-history.js 2024-07-01` + `scrape-telegram-history.js 2024-07-01` 실행 → **history 735일(2024-07-01~2026-07-23), 방향성 의견 8,764건 전부 종가 보유(누락 0)·필드결손 0**. **1년 적중률 13/16 소스 표본충족**: 에테르 56.3%(64)·IT는SK 57.9%(577)·잠실개미 46.7%(321)·피카츄 55.4%(413)·캬오 38.5%(244)·펭미업 44.3%(122) 등. 아직 대기 3: 의교창·투자콤(방향성 의견이 최근에만)·선진짱(표본 극소).
- **[치명적 버그 수정] 시세/지수 조용한 실패**: 1차 2년 백필이 exit 0이었지만 실제로는 시세 1316개 중 273개만 성공, **2024년 지수 0개**(→적중 계산 불가로 2024 의견 전량 누락). 원인=종목 연타로 네이버 API 레이트리밋, `catch{}`가 실패 삼킴. 수정: `fetchJSON`에 지수백오프 재시도(429·5xx·타임아웃), **지수를 종목루프보다 먼저 수집**, 시세/지수 실패 카운트+경고. 재실행 후 지수 505거래일·종가 888종목(실패 1)로 정상화.
- 설정: history 보관 400→**760일**(2년 전체 보존), 텔레 MAX_PAGES 80→**400**(고빈도 채널 과거까지, 대부분 401p까지 긁음). PRICE_DAYS 380→**760**(2년 시세, siseJson 단일호출 506거래일 실측).
- **history.json minify**(무손실): pretty→`JSON.stringify()`, 24.3→**17.5MB**. 4개 쓰기 지점(collect-rss·scrape 2종·backfill) 전부 minify. 프론트 미사용 기계전용 파일이라 안전.
- 백필 분석실패 20/2345건(0.85%, 모델거부·JSON잘림·유니코드 — `_failed`로 캐시 안 됨, 무해). 회귀 46/46.
- **순위를 1년 적중률순으로 변경 완료**(커밋 275438f, 사용자 결정): hitrate.js 정렬을 최장창(1년) 우선 → 3개월 보조 → 대기(장기표본부족) 하위. SourceScores 헤더 "1년 적중률순" 표기. 라이브 1위 너는무한 68.8%. **주의**: 소수표본 소스가 상위 올 수 있음(금싸 66.7% n=12, 너는무한 n=32) — 아래 ⓑ 참조.
- **MIN_SAMPLE 5→20 적용 완료**(커밋 953f993, 사용자 결정): `computeSourceScores(history, windows, minSample=20)` 파라미터화. n<20은 "표본 적음"→순위 제외. 효과: 금싸(1년 n=12) 2위→13위. 골든테스트는 minSample=5 명시로 유지, 회귀 47/47. 라이브 확인.
- **미결/제안(사용자 검토 요청)**: ⓒ history 17.5MB 여전히 큼 → 종가를 "필요 셀만"(의견일+평가시점) 남기는 정밀 트림하면 대폭 감소 가능하나 위험/복잡 — 미적용.

**세션 14 (2026-07-22)**:
- **적중률 창 3개월·1년 2개로 확정** (커밋 26fdcb4): 사용자 의도 = "오늘 기준 지난 1년간 이 채널이 얼마나 맞았나"로 채널 순위 평가. 1개월 제거, `DEFAULT_WINDOWS=[63,252]`. **3개월=근거리 신뢰 랭킹(정렬 1순위, 표본 179~618건)**, 1년=장기 성적표(축적되며 채워짐, 현재 7소스 표본충족). 라이브 IT는 SK 3개월 67%(618) 1위. 회귀 46/46. (개념정리: "1년"은 평가기간=252거래일 보유이지 표본기간 아님. 최근 의견 90%가 아직 252일 미경과라 1년칸 "결과 대기"는 정상이며 시간이 지나며 채워짐.) **보류(2026-07-22 사용자 결정: 지금은 그대로 둠)**: ⓐ용량정리 minify(7.74→3.75MB, 무손실·안전, 필요시 `JSON.stringify(h,null,2)`→`JSON.stringify(h)`) — 급하지 않아 보류. ⓑ순위용 MIN_SAMPLE 상향(현재 5, n=5 우연 방지용 20~30) — 3개월은 이미 표본 충분(179~618건)이라 불필요, 1년 데이터 무르익으면 재검토. **오래된 데이터 삭제는 하지 말 것**(1년창 재료를 없앰). "결과 대기"는 버그 아님 — 1년전(2025-11-11) 이전 방향성 의견 없는 소스가 1년칸 대기, 고빈도 텔레(에테르 2026-05-20~·펭미업 2026-06-01~)는 MAX_PAGES=80 한계로 최근 2개월만 긁혀 3개월칸도 대기.
- **1년치 소급 백필 완료** (커밋 2e7fde0): `scrape-history.js 2025-07-21`(블로그, 319일 의견) + `scrape-telegram-history.js 2025-07-21`(텔레) 실행 → **history 346거래일(2025-07-21~2026-07-22), 7.48MB**. 1년 창 **7개 소스 표본충족**: IT는 SK 65.8%(149)·너는무한 73.3%(15)·너쟁이 65.2%(23)·콤디티 30%(50)·씹배 30.8%(26)·피터케이 16.7%(12)·Teddy 0%(5). 필드결손 0, 라이브 확인.
- **1년 백필용 하드 블로커 4개 수정**(커밋 845b9bc): ①`archiveHistory` 보관 120→**400일**(안 그러면 일일 파이프라인이 백필을 다음날 잘라냄, 치명적) ②scrape 두 스크립트 `PRICE_DAYS` 110→**380**(시세 1년 커버, siseJson 단일호출 ~255거래일 실측 확인) ③scrape prune 200→400 ④로그 w5/w20→w[N]. 사용자 결정: 보관 400일, 텔레 MAX_PAGES=80 유지(부분커버 감수).
- **백필 중 API 과부하(529) 장애 → 재시도+오염방지 도입**: `analyzePost`가 실패를 삼켜 빈값 캐시→재시도 안 되는 **조용한 오염** 발견. 대응: analyzePost에 지수백오프 재시도(5회, 529·timeout·connection 대상), 실패 시 `_failed` 표식→scrape가 캐시 안 함. 오염 캐시 92건 제거 후 재실행 성공. (남은 실패 2건=모델 거부응답·잘못된 유니코드, 재시도 대상 아님, 캐시 안 됨→다음 실행 재시도. 무해.)
- **주의(신규)**: ⓐ history.json이 **7.48MB**로 커져 매일 Actions가 커밋 → 리포 용량 증가(사용자 400일 결정). ⓑ 1년 표본은 아직 작음(가장 오래된 의견만 성숙) → 시간+백필로 증가. ⓒ 고빈도 텔레(에테르·펭미업)는 MAX_PAGES=80 한계로 3개월/1년 표본부족(0/0). ⓓ 데이터 충돌 잦음 — 이번에도 일일 Actions 커밋과 posts/history 충돌 → history=백필본, posts=오늘 신선데이터+백필 source_scores 재계산으로 병합.

**세션 13 (2026-07-21)**:
- **조용한 실패 감지 추가**(개선 A, 위 취약점 해소): `fetchTelegramChannel`이 리다이렉트 최종 URL로 프리뷰 꺼짐(`previewOff`)을 판별해 `{html, previewOff}` 반환. 순수 함수 `classifyTelegramHealth({previewOff,parsedCount,windowCount})` → `preview-off`(수집불가·경고) / `parse-empty`(구조변경·차단 의심·경고) / `no-recent`(저빈도 정상·정보) / `ok`. 수집 루프가 문제 채널을 모아 `console.warn` + `notifyTelegramAlert()`로 텔레그램 경보(BOT_TOKEN/CHAT_ID 있을 때). 저빈도 채널의 "오늘 글 없음"은 정상으로 구분해 오경보 방지. 회귀 TG-H1~5 추가(45/45). 라이브 검증: kimcharger=preview-off, comvestment=정상. **개선 B(프리뷰 꺼진 채널 실제 수집=MTProto)는 인프라 과대라 보류** — 필요 시 채널 주인에게 프리뷰 활성 요청이 최선.

**세션 16 (2026-08-07, Codex)**:
- 수집기 개선: 자동 수집 지연 원인 확인, React 중복 key 수정, 개별 글 24개씩 점진 로딩, `collect-rss.js` 파서 모듈 1차 분리 및 회귀 테스트. 커밋 `6da4e7b`.
- **피터케이 Fear & Greed BETA**: 기존 지수 UI를 대체해 피터케이 시장 관점 글만 집계. 공포~탐욕 점수·근거·표본 신뢰도를 표시하되 2년 백테스트 전 실험 지표임을 명시. 커밋 `74bbd82`.
- **소스 우선 탐색 UI**: 소스 적중률을 헤더 다음 최상단으로 이동. 윌슨 하한으로 표본 수를 보정한 `종합 신뢰도`(3개월 40% + 1년 60%)와 1년·3개월 정렬 제공. 상위 5개 소스별 최근 7일 관련글 최대 2개 연결, 소스 클릭→개별 글 필터, 관련글 클릭→상세 모달. 전체 성적표는 접힘 영역으로 유지.
- 순위·관련글 순수 함수: `src/utils/source-ranking.js`. TDD 회귀 테스트 **64/64**, 프로덕션 빌드 성공, 데스크톱·모바일 반응형 및 콘솔 오류 0건 확인.
- 운영 배포 완료: 커밋 `610d3d1`, Vercel HTTP 200 및 신규 번들 문구 확인.
- 다음 검토 후보: 소스 적중률·관련글 중심 구성이 실제 사용 빈도를 높이는지 며칠간 관찰. 기능 추가 전 사용성부터 확인할 것.

**세션 17 (2026-08-09, Codex)**:
- 사용자 목표를 "전체 글 대시보드"에서 **30초 안에 읽을 원문을 고르는 투자 아이디어 선별기**로 구체화. 사용 시점은 장 시작 전·장 마감 후, 신뢰 기준은 종목 의견의 1년 적중률, 관심 종목은 삼성전자·SK하이닉스.
- `DecisionCockpit.jsx` 신설: 최근 2일 투자 무관 글과 관심 종목을 제외한 서로 다른 신규 아이디어 원문 3개, 관심 종목의 최근 7일 강세·약세 최강 근거, 종목별 상반 의견의 양쪽 최강 근거를 첫 화면에 표시.
- `decision-dashboard.js` 신설: 선별·관심종목·의견충돌·장 시간대 판정을 순수 함수로 분리. 모든 우선순위는 1년 적중률을 윌슨 하한으로 표본 보정하며, AI 재호출 없음.
- `SourceScores.jsx`의 기본 탭을 종합 신뢰도에서 **1년 적중률**로 변경. 기존 전체 리포트·소스 순위·개별 글 기능은 보존.
- TDD: DD1~DD6 추가, 전체 회귀 **70/70**, 프로덕션 빌드 성공. 실제 데이터 144개로 데스크톱·모바일 375px 렌더, 가로 넘침 0, 관심 종목→종목 리포트 연결, 브라우저 경고·오류 0 확인.
- 후속: 상위 신뢰 소스 새 글 외부 알림은 채널·임계값 확정 후. 피터케이 극단적 공포의 5거래일 성과는 일별 지수 데이터 축적 후 검증.

**세션 18 (2026-08-09, Codex)**:
- 실데이터로 30초 선별 화면 재감사. 발견한 오류: 아이디어 3개 중 동일 소스 중복, 관심 종목과 직접 관련 없는 일반 글이 최강 근거로 선택, 같은 필자의 시각 변화가 소스 간 의견 충돌로 표시, 중립 소스까지 비교 소스 수에 포함.
- 선별 수정: 1년 적중률이 검증된 소스를 최우선으로 유지한 상태에서 가능한 경우 서로 다른 소스 3개를 선택. 미검증 소스는 검증 소스 아이디어가 부족할 때만 사용.
- 관심 종목 수정: 제목·요약·판단 근거에서 종목을 직접 언급한 글을 우선. 직접 언급이 없어 폴백한 경우 UI에 `함께 언급(맥락 확인)` 표시.
- 의견 충돌 수정: 서로 다른 필자의 강세·약세 조합만 표시하고, 비교 소스 수는 방향성 의견만 집계.
- 사용성 측정: 날짜별 원문 클릭 수를 기기 localStorage에 저장해 `원문 0/3 확인`으로 표시. 서버 전송·AI 호출 없음.
- 검증: DD2b~DD5c 회귀 추가, 전체 **75/75**, 실데이터에서 아이디어 소스 3개 분산·충돌 양쪽 필자 상이 확인. 프로덕션 빌드 성공, 모바일 375px 가로 넘침 0, 클릭 후 0/3→1/3, 브라우저 오류 0 확인.

**세션 19 (2026-08-09, Codex)**:
- 첫 화면을 사용자가 확정한 **관심 종목 중대 변화 1개 → 완전 신규 2개 → 재부상 1개** 구조로 교체. 조건 미달 시 억지로 채우지 않으며, 실데이터 기준 현재는 재부상 1건만 노출되는 것이 정상.
- `shared/discovery.js` 신설: 45일 경량 언급 이력, 최근 30일 언급 판정, 필자별 이전 방향, 촉매 추출을 공용 순수 함수로 분리. 프론트는 18MB `history.json`을 받지 않고 `posts.json`의 `mention_history`만 사용.
- Git의 `posts.json` 변경 이력 99개 중 읽을 수 있는 98개 스냅샷·고유 글 714개를 합쳐 최초 45일 언급 이력을 복원. 이후 일일 수집기가 자동으로 병합·정리하므로 별도 백필은 불필요.
- 기존 Claude 분석 호출에 `catalyst` 1문장만 추가해 별도 API 호출이나 토큰 왕복을 만들지 않음.
- 첫 화면은 압축 목록·원문 바로 이동·열람 항목 흐림 처리. 관심 종목은 접힌 요약 후 터치해 근거를 펼치며, 의견 충돌은 데스크톱 좌우·모바일 480px 이하 위아래 비교. 오늘 선별 비율·최근 7일 이용일·원문 클릭 수는 기기 로컬에만 저장.
- TDD: DISC1~DISC10 추가, 전체 **85/85** 통과. 프로덕션 빌드 성공(47 modules, JS 244.36kB), 데스크톱 가로 넘침 0px, 관심 종목 펼침·양쪽 근거·원문 링크 확인. 모바일은 브라우저 창 크기 변경 제약으로 실제 캡처 대신 480px 미디어 규칙과 DOM 구조를 검증.

**세션 20 (2026-08-09, Codex)**:
- 사용자 피드백에 따라 첫 화면의 중심을 `오늘 새로 볼 것`에서 **오늘 종합의견**으로 변경. `report-layout`(종합 리포트+팩트 사이드바)을 최상단으로 이동하고 1+2+1·관심 종목·의견 충돌·소스 적중률은 아래 보조 영역으로 보존.
- `src/utils/brief-sources.js` 신설: 최신 종합의견의 소수·역발상 최대 2건, 긍정 핵심 1건, 부정 핵심 1건을 최근 2일 글에서 매칭. 종목·근거 문장 일치도를 확인하고 같은 주제에서는 검증된 1년 소스를 우선하며, 무관 글과 URL 중복은 제외.
- `DailyBrief.jsx`에 **중요·특이 의견 원문** 최대 4개를 추가. 각 행에서 의견 유형·핵심 근거·필자·1년 적중률/표본·날짜를 확인하고 원문으로 직접 이동.
- 선호 블로그 가중치는 확증편향을 막기 위해 이번 핵심 선별 기준에 넣지 않음. 필요 시 이후 별도 `즐겨찾기` 표시로 추가하되 전체 소스 비교는 유지할 것.
- 실데이터 결과: 잠실개미·펭미업 소수의견, 의교창 팔란티어 긍정, 에테르 마이크론 부정 원문이 연결됨. BR1~BR6 추가, 전체 **91/91** 통과, 프로덕션 빌드 성공(48 modules, JS 248.01kB), 데스크톱 첫 순서·원문 4개·가로 넘침 0px·오류 오버레이 없음 확인.

**세션 21 (2026-08-12, Codex)**:
- 국내 시세를 API 키 없는 한국주식데이터(aikstockdata.com) 정적 JSON 우선으로 전환. 새 모듈 `scripts/lib/market-data.js`에서 종목코드·스키마·수치·기준일을 검증하고 8일 이상 지연 데이터는 거부한다.
- 국내 종목은 `s/{code}_history.json`을 우선 사용하고 실패하면 기존 네이버 시세로 자동 폴백한다. 2년 백필처럼 365일 초과 요청은 공개 파일의 250거래일 한계 때문에 기존 경로를 유지한다.
- 국내 지수는 `today.json`의 KOSPI·KOSDAQ 최신 확정값을 기존 네이버 지수 이력에 병합한다. 공개 JSON이 실패하면 네이버 값만 사용하며, 투자자별 수급과 해외 시세는 변경하지 않았다.
- 기존 GitHub Secret `CLAUDE_API_KEY`는 블로그 요약·종합의견에 그대로 사용하며 새 키·MCP·n8n은 추가하지 않았다.
- AIK1~AIK9 테스트 추가: 변환, 코드 불일치, 지연, 잘못된 종가, 지수 변환·병합, 네이버 폴백, 기준일 역전·결측 등락률 방지. 전체 **100/100** 통과, 프로덕션 빌드 성공. 실제 공개 응답으로 삼성전자 230,000원·SK하이닉스 1,420,000원·KOSPI 6,299.66·KOSDAQ 854.47(모두 2026-08-10 기준)을 확인했다.
## 세션 28 (2026-08-12, Codex)
- 투자자 페르소나 기반 원문 판단을 수집 프롬프트에 추가했다. 매수·매도 대신 `반드시 원문 읽기`, `추가 조사하기`, `관심 목록에 저장`, `기존 투자 논리 점검`, `참고만 하기`, `제외하기`로 행동을 분류한다.
- 근거 등급·등급 이유·가장 강한 반대 근거·가격 반영 여부·투자 논리 연결·삼성전자와 SK하이닉스 영향 경로를 구조화해 저장한다. 허용되지 않은 값은 보수적으로 정규화하며 가격 정보가 없으면 `판단 불가`로 유지한다.
- 추천 순위는 AI 판단만 믿지 않고 본문 깊이·근거 품질·1년 소스 적중률을 함께 반영한다. 저신뢰·근거 미확인·제목 기반 글의 `반드시 원문 읽기`는 `추가 조사하기`로 낮춘다.
- 2026-08-11~12 글 29건을 새 스키마로 재분석했다. 행동 분포는 반드시 원문 10, 추가 조사 14, 기존 논리 점검 1, 참고 2, 제외 2이며 누적 글은 141개다.
- 추천 카드에서 행동, 가장 강한 반대 근거, 관심 종목 영향, 근거 등급, 가격 반영 여부를 바로 확인할 수 있다. 기존 추천 3개·피드백·원문 이동·Peter K·소스 적중률은 유지했다.
- TDD: 전체 153/153 통과. AI JSON 잘림 재시도, 분석 실패 시 기존 데이터 보존, 좁은 카드의 라벨 줄바꿈도 함께 검증했다.

## 세션 31 (2026-08-13, Codex)
- 독립 감사에서 확인된 P0만 수정했다. 기능 추가나 전면 UI 재설계는 하지 않았다.
- 종합판단 캐시는 날짜만 보지 않고 현재 글·가격·시장 데이터의 SHA-256 입력 해시를 사용한다. 같은 날 입력이 바뀌면 다시 생성하고 동일하면 재사용한다.
- 첫 화면 정보 위계는 `종합판단 + Peter K → 꼭 읽을 글 → 관심 종목 객관 데이터 → 소스 실험 통계`로 변경했다.
- 텔레그램 일일 묶음은 각 메시지에 번호와 URL을 넣어 분석하고, AI의 `primary_source_index`가 유효할 때 해당 메시지를 원문 링크로 저장한다.
- 분석 스키마를 v3로 올리고 현재 버전의 `source_role=opinion`만 독립 의견 수와 신규 적중률 이력에 포함한다. 따라서 첫 실행 후 과거 구형 판정 수치는 유지하지 않고 새 표본부터 정직하게 재축적한다.
- `소스 적중률`은 `소스 실험 통계`로 표시하며 반복 의견, AI 방향 판정, 국내 종목 KOSPI 단일 벤치마크 한계를 명시했다.
- 회귀 테스트 193/193 및 Vite 프로덕션 빌드 통과. GitHub Actions #105가 27건을 v3로 재분석해 데이터 커밋 `4e90854`를 생성했고 Vercel 실화면에서 정보 순서와 모바일 가로 넘침 없음까지 확인했다.
## 2026-08-15 소스 적중률 정식 재구축 인수인계

- 기존 적중률 0건의 주원인은 데이터 자체 부재가 아니라 구형 의견 11,552건이 현재 규격에서 제외된 것이었다. 구형 의견을 임의 승격하지 않는다.
- 새 계산은 `scripts/hitrate.js`: 적중률 스키마 v1, 게시 다음 실제 거래일부터 63·252거래일, 시장 지수 대비, 7일 독립 에피소드 기준이다.
- 새 백필은 `scripts/backfill-source-history.js`, 수동 워크플로는 `.github/workflows/backfill-source-history.yml`이다. 기본 최근 2년, Claude Haiku, 재개 캐시 사용, 6시간 제한이다.
- 고빈도 텔레그램은 메시지 원문 수와 분석 단위를 구분해 기록하고 인물×날짜 단위로 분석한다.
- 주간 가격 유지 작업은 `scripts/refresh-source-scores.js`와 `.github/workflows/refresh-source-scores.yml`이다. AI를 호출하지 않는다.
- 당시 로컬 테스트 211/211, 빌드 55 modules까지 완료한 상태였다. 이후 2026-08-17 v1.0.0 동결 단계에서 백필·push·운영 화면 검증을 완료했다.
- `.npm-cache/`는 기존 미추적 폴더이므로 계속 제외한다. `n8nversion`은 건드리지 않는다.
